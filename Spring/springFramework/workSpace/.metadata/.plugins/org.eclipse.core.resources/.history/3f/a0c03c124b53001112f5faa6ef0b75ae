package kosta.main;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.EntityTransaction;
import javax.persistence.Persistence;

import kosta.entity.Member;
import kosta.entity.Team;

public class MainApp {

	public static void main(String[] args) {
		System.out.println("===JPA 시작합니다.===");
		
		EntityManagerFactory emf = Persistence.createEntityManagerFactory("JPAProject");
		EntityManager em = emf.createEntityManager();
		EntityTransaction ex = em.getTransaction();
		
		ex.begin();
		
		// 팀 등록
		/*Team t1 = Team.builder().teamName("t1").build();
		Team t2 = Team.builder().teamName("t2").build();
		Team t3 = Team.builder().teamName("t3").build();
		
		em.persist(t1);
		em.persist(t2);
		em.persist(t3);
		
		// 멤버 등록
		em.persist(Member.builder().name("정건").age(22).team(t1).build());
		em.persist(Member.builder().name("나영").age(21).team(t1).build());
		em.persist(Member.builder().name("미미").age(20).team(t1).build());
		
		em.persist(Member.builder().name("효리").age(33).team(t2).build());
		em.persist(Member.builder().name("영식").age(26).team(t2).build());*/
		
		// 회원 검색
		/*Member m = em.find(Member.class, 4L);
		System.out.println("member = " + m);
		
		Team team = m.getTeam();
		System.out.println("team info = " + team);*/
		
		// 팀 검색
		Team t = em.find(Team.class, 1L);
		System.out.println("team = " + t);
		
		System.out.println("------------1------------");
		ex.commit();
		System.out.println("------------2------------");
		
		em.close();
		emf.close();
		
		System.out.println("team.getMemberList = " + t.getMemberList());
		System.out.println("===JPA 끝.===");

	}

}
